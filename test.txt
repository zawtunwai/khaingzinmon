// CLOUDFLARE WORKER CODE FOR: https://github.com/zawtunwai/khaingzinmon
// Custom 404 Page Added
addEventListener('fetch', event => {
  event.respondWith(handleRequest(event.request))
})

async function handleRequest(request) {
  const url = new URL(request.url)
  const path = url.pathname

  // 🎯 BASE URL: ညီလေးရဲ့ GitHub repo
  const GITHUB_RAW_BASE = 'https://raw.githubusercontent.com/zawtunwai/khaingzinmon/main'

  // 🗺️ ROUTE MAP: Worker လမ်းကြောင်း => GitHub မှာရှိတဲ့ ဖိုင်အမှန်တည်နေရာ
  const routeMap = {
    // Main installation script
    '/udp.sh': `${GITHUB_RAW_BASE}/udp.sh`,
    // Telegram Bot
    '/bot.py': `${GITHUB_RAW_BASE}/telegram/bot.py`,
    // Web Panel & Templates (combined in udp.sh, referenced separately if needed)
    '/web.py': `${GITHUB_RAW_BASE}/web.py`, // Assuming it's at root, adjust if inside a folder
    '/index.html': `${GITHUB_RAW_BASE}/templates/index.html`,
    // Protection System
    '/protection.py': `${GITHUB_RAW_BASE}/protection/protection.py`,
    '/self_destruct.sh': `${GITHUB_RAW_BASE}/protection/self_destruct.sh`,
    // Utilities
    '/restart.sh': `${GITHUB_RAW_BASE}/restart.sh`
  }

  // 1. Check if the requested path is in our map
  if (routeMap[path]) {
    const githubUrl = routeMap[path]
    try {
      // 2. Fetch the file directly from GitHub
      const response = await fetch(githubUrl)
      
      // 3. If GitHub returns OK, pass it through with our headers
      if (response.ok) {
        const modifiedResponse = new Response(response.body, response)
        // Add security/identification headers
        modifiedResponse.headers.set('X-Served-By', 'ZIVPN-Cloudflare-Shield')
        modifiedResponse.headers.set('Cache-Control', 'public, max-age=14400') // Cache for 4 hours
        return modifiedResponse
      } else {
        // If GitHub returns an error (e.g., 404), pass it through
        return response
      }
    } catch (error) {
      // Handle network errors
      return new Response(`Error fetching from GitHub: ${error.message}`, { status: 500 })
    }
  }

  // 4. If path not found in our map, return CUSTOM 404 PAGE
  return new Response(
`<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZIVPN Secure Delivery</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', 'Padauk', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #e2e8f0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
            overflow: hidden;
        }
        
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 80%, rgba(59, 130, 246, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(139, 92, 246, 0.1) 0%, transparent 50%);
            z-index: 0;
        }
        
        .container {
            position: relative;
            z-index: 1;
            max-width: 800px;
            width: 100%;
            text-align: center;
            animation: fadeIn 0.8s ease-out;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .error-code {
            font-size: 120px;
            font-weight: 900;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1;
            margin-bottom: 10px;
            text-shadow: 0 5px 15px rgba(59, 130, 246, 0.3);
        }
        
        .title {
            font-size: 28px;
            color: #f8fafc;
            margin-bottom: 30px;
            font-weight: 600;
        }
        
        .brand-box {
            border: 2px solid;
            border-image: linear-gradient(135deg, #3b82f6, #8b5cf6) 1;
            padding: 20px 40px;
            margin: 30px auto;
            max-width: 500px;
            position: relative;
            background: rgba(30, 41, 59, 0.7);
            backdrop-filter: blur(10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }
        
        .brand-box::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            z-index: -1;
            filter: blur(10px);
            opacity: 0.4;
        }
        
        .brand-name {
            font-size: 22px;
            font-weight: 700;
            color: #f1f5f9;
            letter-spacing: 2px;
        }
        
        .creator {
            font-size: 18px;
            color: #10b981;
            font-weight: 600;
            margin-top: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        
        .flag {
            font-size: 24px;
            animation: wave 3s infinite;
        }
        
        @keyframes wave {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(5deg); }
            75% { transform: rotate(-5deg); }
        }
        
        .message {
            background: rgba(30, 41, 59, 0.8);
            border-radius: 12px;
            padding: 25px;
            margin: 30px auto;
            max-width: 600px;
            border-left: 4px solid #3b82f6;
            text-align: left;
        }
        
        .message h3 {
            color: #60a5fa;
            margin-bottom: 15px;
            font-size: 18px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .paths {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 10px;
            margin-top: 15px;
        }
        
        .path-item {
            background: rgba(59, 130, 246, 0.1);
            padding: 10px;
            border-radius: 6px;
            font-family: 'Courier New', monospace;
            font-size: 14px;
            text-align: center;
            transition: all 0.3s;
            border: 1px solid rgba(59, 130, 246, 0.2);
        }
        
        .path-item:hover {
            background: rgba(59, 130, 246, 0.2);
            transform: translateY(-2px);
            border-color: #3b82f6;
        }
        
        .footer {
            margin-top: 40px;
            color: #94a3b8;
            font-size: 14px;
            border-top: 1px solid rgba(148, 163, 184, 0.2);
            padding-top: 20px;
        }
        
        .security-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(16, 185, 129, 0.1);
            padding: 8px 16px;
            border-radius: 20px;
            color: #10b981;
            font-weight: 600;
            margin-top: 15px;
        }
        
        .glow {
            animation: glow 2s infinite alternate;
        }
        
        @keyframes glow {
            from { text-shadow: 0 0 5px rgba(59, 130, 246, 0.5); }
            to { text-shadow: 0 0 15px rgba(59, 130, 246, 0.8), 0 0 20px rgba(59, 130, 246, 0.6); }
        }
        
        @media (max-width: 600px) {
            .error-code { font-size: 80px; }
            .title { font-size: 22px; }
            .brand-name { font-size: 18px; }
            .paths { grid-template-columns: repeat(2, 1fr); }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-code glow">404</div>
        <h1 class="title">ZIVPN Secure Delivery Network</h1>
        
        <div class="brand-box">
            <div class="brand-name">ZIVPN ENTERPRISE</div>
            <div class="creator">
                <span>🧑‍💻 SCRIPT BY</span>
                <span>မောင်သုည</span>
                <span class="flag">🇲🇲</span>
            </div>
        </div>
        
        <div class="message">
            <h3><i class="fas fa-exclamation-triangle"></i> File Not Found</h3>
            <p>The requested path was not found in the secure delivery network. Please use one of the valid paths below:</p>
            
            <div class="paths">
                <div class="path-item">/udp.sh</div>
                <div class="path-item">/bot.py</div>
                <div class="path-item">/web.py</div>
                <div class="path-item">/index.html</div>
                <div class="path-item">/protection.py</div>
                <div class="path-item">/self_destruct.sh</div>
                <div class="path-item">/restart.sh</div>
            </div>
        </div>
        
        <div class="security-badge">
            <i class="fas fa-shield-alt"></i>
            <span>Cloudflare Protected Delivery</span>
        </div>
        
        <div class="footer">
            <p>© 2024 ZIVPN Enterprise. All rights reserved.</p>
            <p>This is a secure, encrypted content delivery system.</p>
        </div>
    </div>
    
    <script>
        // Add Font Awesome for icons
        const faLink = document.createElement('link');
        faLink.rel = 'stylesheet';
        faLink.href = 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css';
        document.head.appendChild(faLink);
        
        // Add subtle animation to path items
        document.querySelectorAll('.path-item').forEach((item, index) => {
            item.style.animationDelay = `${index * 0.1}s`;
            item.style.opacity = '0';
            item.style.animation = `fadeIn 0.5s ease-out ${index * 0.1}s forwards`;
        });
        
        // Add CSS for path item animation
        const style = document.createElement('style');
        style.textContent = \`
            @keyframes fadeIn {
                to { opacity: 1; transform: translateY(0); }
            }
            .path-item {
                opacity: 0;
                transform: translateY(10px);
            }
        \`;
        document.head.appendChild(style);
    </script>
</body>
</html>`,
    { 
        status: 404,
        headers: { 
            'Content-Type': 'text/html; charset=utf-8',
            'Cache-Control': 'no-cache, no-store, must-revalidate'
        }
    }
)
}
